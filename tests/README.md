# Integration tests

End-to-end and contract tests can live here. Package-level tests currently run via:

- `pnpm --filter @creditkit/types test`
- `pnpm --filter @creditkit/backend test`
- `pnpm --filter @creditkit/frontend test`

Add Playwright or API contract suites here as the platform grows.
