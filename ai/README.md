# CreditKit AI module

Server-side AI integration lives in the **backend** (`@creditkit/backend`) using the OpenAI SDK when `OPENAI_API_KEY` is set.

This folder documents prompts and future OCR / document pipelines. Keep API keys only in environment variables — never commit secrets.
